// Exercício (03) - É uma bucha ou não? (dominó)

// Variavel representando a peça;
const ladoA = 3;
const ladoB = 3;

// Condição para saber se é uma bucha ou não.
if (ladoA == ladoB) {
    console.log('Sim, é uma bucha.'); // Imprime no console;
} else {
    console.log('Não, não é uma bucha.'); // Imprime no console;
}