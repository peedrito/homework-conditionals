// Exercício (15) - Imprimindo nome;

// variavel criado pela a escola;
const primeiroNome = "";
const sobrenome = "";
const apelido = "";

if (apelido) {
    console.log(apelido);
} else if (sobrenome) {
    console.log(primeiroNome + " " + sobrenome);
} else {
    console.log(primeiroNome);
}

